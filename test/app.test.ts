describe('Test', () => {
  it('should return true', () => {
    expect(true).toBe(true);
  });
});

/*
describe('Drink', () => {
  it('should get all drinks from database', async () => {
    const drinks = await Drink.find();
    expect(drinks).toBeDefined();
  });
  it('should get one drink from database', async () => {
    const drink = await Drink.findOne({ _id: '644c26ea111203cf91b37c44' });
    expect(drink).toBeDefined();
  });
});

*/
